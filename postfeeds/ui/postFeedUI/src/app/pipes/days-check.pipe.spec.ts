import { DaysCheckPipe } from './days-check.pipe';

describe('DaysCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
